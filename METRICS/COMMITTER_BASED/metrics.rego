package play
import data.metrics
import input
foo[input.committer[i].metrics] {
    input.committer[i].branch_name == data.committer.branch_name
    input.committer[i].name == data.committer.name
    
    }